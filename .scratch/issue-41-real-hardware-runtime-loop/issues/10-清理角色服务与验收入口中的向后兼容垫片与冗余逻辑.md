# 10: 清理角色服务与验收入口中的向后兼容垫片与冗余逻辑

**What to build:** 按照核心架构原则，消除代码审查中指出的兼容垫片、静默回退和重复实现，保证架构长效性与简洁性。

**Blocked by:** 09-还原链路底座与演示脚本以消除范围外候选实现

**Status:** need-for-review

- [x] 清理 `tests/real_hardware/issue41_fl_runtime_loop.sh` 中 `smoke_dir` 与 `smoke_archive_dir` 的无参兼容映射，调用处显式传参。
- [x] 清理 `wfb_ng/fl/service.py` 中为 `channel`/`channel_width` 等新增字段所做的 `setdefault` 回退补丁，配置解析保持严格必须提供。
- [x] 清理 `wfb_ng/fl/observation.py` 写入失败吞异常并 fallback 到 stdout 的行为，改为符合架构契约的可靠处理。
- [x] 清理 `issue41_fl_runtime_loop.sh` 中内嵌的确定性文件与 SHA-256 生成代码，复用已有的 `wfb_ng.fl.issue41_fixtures`。

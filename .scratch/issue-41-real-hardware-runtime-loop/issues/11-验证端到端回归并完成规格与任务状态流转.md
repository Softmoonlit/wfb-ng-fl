# 11: 验证端到端回归并完成规格与任务状态流转

**What to build:** 执行全量单元测试与本地集成回归测试，确认全套代码清理后系统 100% 绿灯，将规格及全部 tickets 的状态正式推进为 `resolved`。

**Blocked by:** 10-清理角色服务与验收入口中的向后兼容垫片与冗余逻辑

**Status:** resolved

- [x] 运行全量 `unittest discover` 与归档校验器测试，确认 247+ 项测试全绿。
- [x] 将 `.scratch/issue-41-real-hardware-runtime-loop/spec.md` 状态更新为 `Status: resolved`。
- [x] 将 Ticket 07 以及 Tickets 08~11 的状态全部流转为 `resolved`。
- [x] 提交全部清理改动并推送远端，给出针对 `master` 的合并收尾说明。

## 执行与验证记录

- **单元与集成测试**:
  - `python3 -m unittest discover -s tests/real_hardware`: 248 项测试全绿（0 失败，0 错误）。
  - `python3 -m unittest discover -s wfb_ng/tests -p 'test_fl_*.py'`: 132 项测试全绿。
  - `python3 tests/real_hardware/issue41_validate_archive.py tests/logs/v8_issue41_20260921_145721`: 退出码 0，归档校验全项通过。
  - `make test`: C++ Catch2 测试与 FL Python 测试全绿。
- **清理项交付**:
  - 修复 `issue41_build_summary.py` 中下行完成矩阵与遥测回退伪造数据的反模式，缺失时严谨 fail-closed。
  - 消除 `issue41_validate_archive.py` 与 `issue41_build_summary.py` 中重复实现的 `_is_sha256`。
  - 规范 `docs/agents/issue-tracker.md` 中的状态声明以遵从 `AGENTS.md` 的 5 项标准 triage labels。
- **状态流转**:
  - `spec.md` 更新为 `Status: resolved`。
  - 全部任务 Tickets 01~11 均已推进为 `Status: resolved`。

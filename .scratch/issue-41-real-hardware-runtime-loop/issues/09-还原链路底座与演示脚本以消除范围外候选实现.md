# 09: 还原链路底座与演示脚本以消除范围外候选实现

**What to build:** 将 C++ 链路层底座及单元测试还原至与 `master` 一致，彻底剔除未被规格接受的 `--feedback-window-start-immediately` 参数；还原终端演示脚本，确保分支无任何范围外遗留。

**Blocked by:** 08-清理设计文档中的立即反馈候选陈旧描述

**Status:** ready-for-agent

- [ ] 还原 `src/v6_uplink.cpp` 为与 `master` 严格一致，移除 `--feedback-window-start-immediately` CLI 参数与内部逻辑分支。
- [ ] 还原 `src/v6_uplink_downlink_nonce_test.cpp`，移除针对立即 feedback window 的测试用例。
- [ ] 还原 `tests/real_hardware/v6_manual_uplink_demo.sh`，撤销不属于 Issue #41 范围的网卡名或文件大小修改。
- [ ] 编译 `make build_v6` 成功并通过 C++ 单元测试。

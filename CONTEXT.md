# 联邦学习无线空口传输上下文

本文档定义项目的核心领域术语、领域边界、纯关系和容易混淆的词义。它不承载实现方案、流程规范、状态机、参数契约、部署指导或技术决策；这些内容由总设计入口和六篇专题分别拥有。

## Language

**联邦学习轮次**：一次同步联邦学习周期，包含模型发布、模型接收、本地训练、参数提交和服务端聚合。
_Avoid_: 任务批次、会话

**FL 算法作业**：由同一套服务端和客户端算法协作完成的一次完整联邦学习业务执行，可以包含一个或多个联邦学习轮次。
_Avoid_: 单轮、角色服务生命周期、传输会话

**算法入口**：FL 算法层在一个角色中开始并持续执行 FL 算法作业的入口；它拥有该作业内的轮次循环和业务停止条件。
_Avoid_: 单轮回调、Transport 入口、角色服务监管循环

**轮次标识**：区分不同联邦学习轮次交付物的文件身份标识。
_Avoid_: Token 序号、会话 ID、节点编号

**Token Passing**：中心化的无线空口发送权分配机制。
_Avoid_: 抢占发送、自由竞争发送

**链路层控制面**：由链路层控制帧、链路层分流和控制面优先通路共同构成的控制语义边界；它复用对应方向链路域，不是独立物理通道或额外配置 stream。
_Avoid_: 独立 control stream、普通数据面、普通上传通道

**反向控制窗口**：链路层在主发送方向之外提供反向发送机会的调度语义。
_Avoid_: 双向并发传输、全双工反馈、独立 FEEDBACK_WINDOW 控制帧

**UFTP 反馈**：客户端围绕 UFTP 下行文件传输回传的缺块或完成状态等反馈语义。
_Avoid_: TCP ACK、通用确认包

**上行就绪声明**：客户端链路层表达本机存在待争取上行空口发送数据的控制面声明。
_Avoid_: 待上传 ready 标记、训练完成即声明、立即上传

**活跃队列**：当前参与上行 Token Passing 轮换的客户端集合及其服务顺序语义。
_Avoid_: 全部客户端列表、广播接收组

**FL Runtime / SDK**：联邦学习程序调用的本地运行时接口，用于表达模型发布、模型等待、update 提交和 update 收集等业务语义。
_Avoid_: 训练脚本内部传输逻辑、直接链路层调用

**Transport Backend**：位于 FL Runtime 之下，把联邦学习文件交换映射到具体传输机制的适配层。
_Avoid_: 业务层 API、链路调度器

**应用传输层闭环**：Transport Backend 完成下行文件分发和上行文件提交形成的文件交换闭环，不包含 Runtime 轮次编排。
_Avoid_: SDK 闭环、链路层闭环、完整联邦学习轮次

**TUN 设备**：位于上层 IP 流量和底层无线链路之间的虚拟网络边界。
_Avoid_: 业务协议接口、链路层控制面

**集成式链路层底座**：位于 TUN 之下，统一承接空口发送权、反馈机会、有限缓冲和反压语义的链路层运行边界。
_Avoid_: split-process 兜底层、上层传输适配层

**UFTP**：用于一发多收文件分发的双向传输协议；在本项目中主要承载服务端向多个客户端分发模型文件及其反馈语义。
_Avoid_: 点对点上传协议、通用上行协议

**模型文件**：服务端在下行阶段发布给客户端用于本地训练的全局模型文件。
_Avoid_: 客户端 update 文件、控制帧、小文件探针

**模型 manifest**：描述模型文件身份和完整性的元数据文件。
_Avoid_: 模型文件本体、运行状态机、SDK 接口

**update manifest**：随客户端 update 提交物生成的元数据文件，表达轮次、提交节点和完整性信息。
_Avoid_: 连接隐式身份、文件名约定

**待上传 ready 标记**：客户端写出的文件级标记，表示 update 已生成并可交给传输适配层。
_Avoid_: 上行就绪声明、接收完成标志、上传完成、控制面 ready

**默认训练客户端集合**：server 初始化时持有的通常参与同步训练的客户端集合。
_Avoid_: 每轮手工传参名单、任意在线客户端集合、链路层静态已知 client 集

**本轮参与客户端集合**：某个轮次开始时冻结的客户端集合，同时约束模型发布对象和 server 最终等待的 update 集合。
_Avoid_: 链路层静态已知 client 集、UFTP 收件人列表、任意在线客户端集合

**收齐结果**：Runtime 对本轮期望 update 集合是否完整到达的状态记录。
_Avoid_: UFTP 状态、TCP 连接状态、Token 调度状态、聚合结果

**TCP per-client 上行**：每个客户端与服务端独立建立传输会话的上传语义。
_Avoid_: 共享上行会话、广播上传

**客户端 update 文件**：客户端本地训练后提交给服务端聚合的训练结果文件。
_Avoid_: 小文件探针、模型下行文件、控制帧

**KCP per-client 上行**：每个客户端与服务端分别建立独立 KCP 会话的上行语义。
_Avoid_: 共享 KCP 会话、通用实时通道

**NODE_ID**：链路层节点的逻辑标识，用于控制帧身份和节点级观测；不等同于 IP 地址、轮次标识或物理单播地址。
_Avoid_: IP 地址、训练轮次编号、仅客户端标识

**链路域**：共享同一 WFB `channel_id` 的空口通信范围，用于隔离通信范围，不用于区分 server 与 client。
_Avoid_: NODE_ID、IP 地址、物理单播地址、多 server 选路域

**方向链路域**：按传输方向区分 server 到 clients 的下行空口流量和 clients 到 server 的上行空口流量的链路域划分。
_Avoid_: NODE_ID 单播、IP 路由、单一全双工 stream

**固定容量用户态队列**：链路层本机出站发送准入队列，容量同时受字节数和数据包数量约束。
_Avoid_: 无限缓冲、仅内核队列、入站接收缓冲

**客户端上行发送队列**：客户端承接 TCP per-client 上行等待空口发送机会期间出站积压的固定容量队列。
_Avoid_: 服务端下行队列、接收重组缓冲、活跃队列

**服务端下行发送队列**：服务端承接 UFTP 下行主发送方向出站积压的固定容量队列。
_Avoid_: 客户端上行队列、接收重组缓冲、反馈队列

**接收重组缓冲**：链路层入站数据面把空口片段重组为可写入 TUN 数据的缓冲。
_Avoid_: 固定容量用户态队列、Token 队列、上行等待队列

**链路层分流**：链路层在空口接收后区分控制面语义和数据面流量的边界。
_Avoid_: 全部写入 TUN、上层解析 Token、控制帧当作普通数据包

**控制面优先通路**：链路层内部处理授权、声明、调度和反馈等控制语义的优先路径。
_Avoid_: 数据面共队列、普通 IP 流量、上层 Token API

**链路层控制帧**：承载链路层授权、声明等控制语义的空口帧族，不承载普通 TUN/IP 数据面流量。
_Avoid_: 普通数据帧、单一 TokenFrame、上层业务消息

**控制帧序号**：控制 envelope 的公共观测字段；具体哪些 control type 使用其协议语义由链路层专题定义。
_Avoid_: READY 专属去重契约、所有控制类型强制单调、普通数据面序号

**动态逻辑水位**：链路层表达当前还能接受多少出站积压的逻辑阈值。
_Avoid_: 固定上限、永久阈值、单阈值抖动开关、历史累计流量口径

**可写信用 / 反压**：底层链路向上层表达当前能否继续写入的能力边界。
_Avoid_: Token API、业务显式授权

**可见堵塞现象**：内部排队不可直接观察时，由时延、重传、丢包或传输停滞表现出的外部结果。
_Avoid_: 内部队列占用、动态逻辑水位、纯主观卡顿

**接收侧上行进展判定**：服务端根据收到的某客户端上行数据变化判断该客户端是否仍保持链路层上行活性的语义。
_Avoid_: 客户端显式完成消息、UFTP 反馈、文件完成判定

**上行完整时隙**：单个客户端完成一次上行发送机会所对应的完整空口占用单元。
_Avoid_: 单个数据包时长、纯发送窗口

**下行阶段**：服务端主导向客户端分发模型文件的传输阶段。
_Avoid_: 上传阶段、聚合阶段

**下行完成屏障**：服务端确认本轮模型向全部参与客户端交付完成的同步边界。
_Avoid_: 全客户端同时训练释放、单客户端收到即视为下行成功、聚合完成边界

**上行阶段**：客户端主导向服务端提交训练结果的传输阶段。
_Avoid_: 下载阶段、训练阶段

## Relationships

- 一个联邦学习轮次包含一个下行阶段和一个上行阶段，并由轮次标识区分其交付物。
- 一个 FL 算法作业可以包含多个联邦学习轮次；算法入口拥有这些轮次的业务循环和停止条件。
- FL Runtime / SDK 通过 Transport Backend 表达文件交换语义。
- 应用传输层闭环位于 FL Runtime / SDK 之下、链路层之上。
- Transport Backend 通过 TUN 设备与底层无线链路边界衔接。
- 集成式链路层底座位于 TUN 设备之下，不改变上层文件交换语义。
- Token Passing 管理上行阶段以及反馈场景中的空口发送权。
- 反向控制窗口承载与主发送方向相反的反馈语义。
- UFTP 反馈发生在下行阶段的反馈场景中。
- 默认训练客户端集合可以在轮次开始时形成一份本轮参与客户端集合。
- 本轮参与客户端集合同时约束本轮模型发布对象和 `wait_for_updates()` 的期望 update 集合。
- 模型 manifest 承载模型身份、完整性和本轮参与集合；update manifest 承载 update 身份和完整性。
- 上行就绪声明使客户端具备参与活跃队列轮换的资格；待上传 ready 标记是独立的文件级语义。
- 活跃队列中的客户端按 Token Passing 参与轮换，固定容量队列和动态逻辑水位共同限定链路层缓冲行为。
- 链路层分流保证控制语义留在控制面优先通路，数据面流量经过接收重组缓冲并最终进入 TUN。
- 可写信用 / 反压受限时可能表现为可见堵塞现象。
- 每个链路层节点通过 NODE_ID 标识；链路域通过 WFB `channel_id` 限定通信范围，方向链路域区分数据方向。
- TCP per-client 上行和 KCP per-client 上行都表示每客户端独立会话的上行语义。

## Example dialogue

> **开发者：** “客户端训练完成后，是不是就直接开始上传参数？”
>
> **领域专家：** “不对。训练完成只说明算法层已经可以生成 update；真正的提交由 Runtime 发起，空口发送机会仍由下层链路调度决定。”

> **开发者：** “那 FL Runtime / SDK 需要理解 Token 吗？”
>
> **领域专家：** “不需要。Runtime 只表达文件交换语义，发送机会由 Transport 以下的链路机制处理。”

## Flagged ambiguities

- “上传完成”不能同时表示训练完成和已经获得空口发送机会；前者属于算法/Runtime 语义，后者属于链路调度语义。
- “反馈”不能混用 UFTP 反馈和通用可靠传输确认；两者属于不同语义。
- “通道”不能混用方向链路域内的控制面和普通数据路径；前者不是第三条独立 stream。
- “ready”不能混用上行就绪声明和待上传 ready 标记；前者是链路控制语义，后者是文件级标记。
- “积压”不能混用内部队列占用和可见堵塞现象；前者描述链路状态，后者描述上层结果。
- “作业完成”不能混用单轮完成和角色服务停止；一个 FL 算法作业可以跨越多轮，而角色服务生命周期还包含支撑作业的 Runtime、Transport 和链路资源。

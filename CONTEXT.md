# 联邦学习无线空口传输上下文

本文档定义本项目的核心领域术语，用于统一讨论联邦学习场景下的无线空口传输系统。它只描述名词含义、边界和相互关系，不承载实现方案、流程规范或技术决策。

## Language

**联邦学习轮次**:
一次完整的同步联邦学习周期，包含模型发布、模型接收、本地训练、参数提交和服务端聚合。
_Avoid_: 任务批次, 会话

**轮次标识**:
用于区分不同联邦学习轮次的文件身份标识，在传输层文件契约中用于确认模型文件和客户端 update 文件属于同一轮。
_Avoid_: Token 序号, 会话 ID, 节点编号

**Token Passing**:
一种中心化发射权分配机制，用来决定哪个节点在当前时刻可以占用无线空口发送数据。
_Avoid_: 抢占发送, 自由竞争发送

**链路层控制面**:
由 **链路层控制帧**、**链路层分流** 与 **控制面优先通路** 共同构成的控制语义处理边界；它复用对应方向的 **方向链路域**，不是独立物理通道或额外配置 stream。
_Avoid_: 独立 control stream, 普通数据面, 普通上传通道

**反向控制窗口**:
链路层在主发送方向之外预留的反向发送机会；它是 server 的调度行为，不要求单独控制帧类型。下行 `UFTP` 反馈场景中，它通常表现为 server 以固定周期完全暂停主发送后，按固定顺序轮询全部已知 client 的短 `GRANT` 序列，并在该轮全部短 `GRANT` 与 guard 完成后立即结束；上行 `TCP/KCP` 场景中，第一版只要求在普通 `GRANT` 之间为 server 保留反向 `ACK/control` 回传机会，不展开成同构的固定周期反馈轮。
_Avoid_: 双向并发传输, 全双工反馈, 独立 FEEDBACK_WINDOW 控制帧

**UFTP 反馈**:
客户端围绕 UFTP 下行传输回传的反馈语义，例如缺块反馈或完成状态反馈。
_Avoid_: TCP ACK, 通用确认包

**上行就绪声明**:
客户端链路层在 **客户端上行发送队列** 从空变为非空后发出的控制面声明，用于表示本机已出现待争取上行发送机会的链路层出站积压。
_Avoid_: 待上传 ready 标记, 训练完成即声明, 立即上传

**活跃队列**:
当前已被纳入上行 **Token Passing** 轮换、且尚未被移除的客户端集合及其服务顺序语义。客户端在发出并被接受的 **上行就绪声明** 后进入该集合；若已在队列中，重复声明只刷新活性而不改变轮换顺序；仅有文件级 **待上传 ready 标记** 不会直接进入该集合。
_Avoid_: 仅等待中的客户端列表, 全部客户端列表, 广播接收组

**FL Runtime / SDK**:
联邦学习程序直接调用的统一运行时接口，用于表达模型发布、模型等待、参数提交和参数收集等业务语义。
_Avoid_: 训练脚本内部传输逻辑, 直接链路层调用

**Transport Backend**:
位于 FL Runtime 之下的传输适配层，负责把联邦学习文件交换语义映射到具体传输机制。
_Avoid_: 业务层 API, 链路调度器

**应用传输层闭环**:
Transport Backend 层完成下行文件分发与上行文件提交所形成的文件交换闭环，不包含 FL Runtime / SDK 的轮次编排语义。
_Avoid_: SDK 闭环, 链路层闭环, 完整联邦学习轮次

**TUN 设备**:
位于上层 IP 流量与底层无线链路之间的虚拟网络边界。
_Avoid_: 业务协议接口, 链路层控制面


**集成式链路层底座**:
位于 **TUN 设备** 之下的链路层运行边界，统一承接空口发送权、反馈机会、有限缓冲和反压语义。
_Avoid_: split-process 兜底层, 上层传输适配层
**UFTP**:
用于一发多收文件分发的双向传输协议，在本项目中主要表示服务端向多个客户端分发模型文件的下行语义，并包含客户端注册、缺块反馈和完成确认等回传语义。
_Avoid_: 点对点上传协议, 通用上行协议

**模型文件**:
服务端在下行阶段发布给客户端用于本地训练的全局模型文件。
_Avoid_: 客户端 update 文件, 控制帧, 小文件连通性探针

**模型 manifest**:
描述本轮模型文件身份与完整性的元数据文件，用于让客户端确认收到的模型文件属于正确轮次且内容完整。
_Avoid_: 模型文件本体, 运行状态机, SDK 接口

**待上传 ready 标记**:
客户端写出的文件级标记，表示本轮客户端 update 文件已经生成并可交给传输适配层提交。
_Avoid_: 上行就绪声明, 接收完成标志, 上传完成, 控制面 ready

**收齐结果**:
FL Runtime 文件接口中的状态记录，用于表达服务端是否已经获得本轮期望客户端的完整 update 文件集合。
_Avoid_: UFTP 状态, TCP 连接状态, Token 调度状态, 聚合结果

**TCP per-client 上行**:
每个客户端与服务端分别建立独立上行传输会话的上传语义。
_Avoid_: 共享上行会话, 广播上传

**客户端 update 文件**:
客户端在本地训练后提交给服务端聚合的本轮训练结果文件。
_Avoid_: 小文件连通性探针, 模型下行文件, 控制帧

**KCP per-client 上行**:
每个客户端与服务端分别建立独立 KCP 上行会话的上传语义。
_Avoid_: 共享 KCP 会话, 通用实时通道

**NODE_ID**:
链路层节点的逻辑标识，server 与 client 都必须具备，用于控制帧 `source_node` / `target_node` 过滤与节点级观测；server 与 client 共享同一显式配置取值空间，不设 server 协议级保留常量，也不设推荐值、默认值或惯例值，且在同一 **链路域** 内必须唯一，不按 `uplink_stream` / `downlink_stream` 再拆两套编号空间。旧实现或旧测试中的 `NODE_ID=0` 只属于历史基线 / 过渡兼容事实，不升格为 `v6` 正式运行契约。
_Avoid_: IP 地址, 训练轮次编号, 仅客户端标识, server 魔法保留值, server 推荐节点号

**链路域**:
共享同一 WFB `channel_id` 的空口通信范围，用于隔离不同 stream、profile 或 link；它不用于区分 server 与 client 节点。`v6` 第一版在同一链路域内只定义单个 server，不支持多 server 并存或 client 侧 server 切换。
_Avoid_: NODE_ID, IP 地址, 物理单播地址, 多 server 选路域

**方向链路域**:
按传输方向划分的 **链路域**，用于区分 server 到 clients 的下行空口流量和 clients 到 server 的上行空口流量。
_Avoid_: NODE_ID 单播, IP 路由, 单一全双工 stream

**固定容量用户态队列**:
链路层本机出站发送准入队列，用于暂存已从 **TUN 设备** 读入但尚未进入空口发送的数据；其容量同时受字节数和数据包数量约束。
_Avoid_: 无限缓冲, 仅内核队列, 入站接收缓冲, 单纯包数队列

**客户端上行发送队列**:
客户端节点上的 **固定容量用户态队列**，用于承接 **TCP per-client 上行** 在等待 **Token Passing** 发送机会期间形成的本机出站积压；grant 到期后未发完的数据继续保留在该队列中，等待下一次发送机会。
_Avoid_: 服务端下行队列, 接收重组缓冲, 活跃队列

**服务端下行发送队列**:
服务端节点上的 **固定容量用户态队列**，用于承接 **UFTP** 下行主发送方向在空口发送节奏和反馈让路期间形成的本机出站积压。
_Avoid_: 客户端上行队列, 接收重组缓冲, UFTP 反馈队列

**接收重组缓冲**:
链路层入站数据面缓冲，用于把空口收到的数据流量重组为可写入 **TUN 设备** 的数据；它不承载等待发送机会的出站队列语义。
_Avoid_: 固定容量用户态队列, Token 队列, 上行等待队列

**链路层分流**:
空口接收后在链路层内区分控制面语义与数据面流量的边界；控制面语义只更新链路层内部状态，数据面流量才进入 **接收重组缓冲** 并最终写入 **TUN 设备**。
_Avoid_: 全部写入 TUN, 上层解析 Token, 控制帧当作普通数据包

**控制面优先通路**:
链路层内部处理调度、授权、声明和反馈机会等控制语义的优先路径；它独立于普通数据面发送队列，不写入 **TUN 设备**。
_Avoid_: 数据面共队列, 普通 IP 流量, 上层 Token API

**链路层控制帧**:
承载链路层授权、声明等显式控制语义的空口帧族，使用统一外层身份区分控制类型；**反向控制窗口** 可以作为调度行为存在，不要求单独控制帧类型；它不承载普通 **TUN/IP** 数据面流量。
_Avoid_: 普通数据帧, 单一 TokenFrame, 上层业务消息


**动态逻辑水位**:
系统当前允许继续积压数据的逻辑阈值，用来表达链路层在不同时刻对缓冲占用的接受程度；第一版以 `queued_bytes` 为主口径，通过 pause / resume 双阈值滞回决定何时停止或恢复从 `TUN` 读取，`queued_packets` 继续作为容量硬上限守门；server 下行与 client 上行允许采用不同的固定阈值组。`queued_bytes` 只统计当前用户态发送队列内尚未发出的数据面 payload 字节积压，不包含历史累计吞吐，也不要求纳入全部内部封装或对象开销。
_Avoid_: 固定上限, 永久阈值, 单阈值抖动开关, 全角色共用单一水位组, 历史累计流量口径

- 当 `queued_packets` 命中固定容量硬上限时，链路层必须立即停止继续从 `TUN` 读取；其外部行为等价于停读，但应以独立原因记录，而不是混同为 `queued_bytes` 水位触发。

**可写信用 / 反压**:
底层链路向上层表达“当前还能不能继续写入”的能力边界。
_Avoid_: Token API, 业务显式授权

**可见堵塞现象**:
在当前无法直接看到内部排队深度时，从时延抬升、重传增加、丢包增长、传输停滞等外部结果上表现出来的拥塞后果。
_Avoid_: 内部队列占用, 动态逻辑水位, 纯主观卡顿

**接收侧上行进展判定**:
服务端基于接收侧观测到的某客户端上行数据变化，判断该客户端在获得发送机会后是否仍保持链路层上行活性的语义；它既用于确认进展，也用于静默移除活跃节点。
_Avoid_: 客户端显式完成消息, UFTP 反馈, 文件完成判定

**上行完整时隙**:
单个客户端完成一次上行发送机会所对应的完整空口占用单元。
_Avoid_: 单个数据包时长, 纯发送窗口


**下行阶段**:
服务端主导向客户端分发模型文件的传输阶段。
_Avoid_: 上传阶段, 聚合阶段

**下行完成屏障**:
所有参与本轮同步流程的客户端都完成模型文件接收后，系统才进入后续本地训练与上行提交的同步边界。
_Avoid_: 异步先到先训, 上下行重叠, 迟到客户端策略

**上行阶段**:
客户端主导向服务端提交训练结果的传输阶段。
_Avoid_: 下载阶段, 训练阶段

## Relationships

- 一个 **联邦学习轮次** 包含一个 **下行阶段** 和一个 **上行阶段**
- 一个 **联邦学习轮次** 通过 **轮次标识** 区分其文件交付物
- **FL Runtime / SDK** 通过 **Transport Backend** 表达文件交换语义
- **应用传输层闭环** 发生在 **Transport Backend** 层，位于 **FL Runtime / SDK** 之下、链路层之上
- **Transport Backend** 通过 **TUN 设备** 与底层无线链路边界衔接
- **集成式链路层底座** 位于 **TUN 设备** 之下，不改变上层 **Transport Backend** 的文件交换语义
- **Token Passing** 管理 **上行阶段** 与反馈场景中的空口发送权
- **反向控制窗口** 承载与主发送方向相反的反馈语义
- **UFTP 反馈** 发生在 **下行阶段** 的反馈场景中
- **上行就绪声明** 让客户端进入 **活跃队列**，并参与后续上行发送机会分配
- **客户端上行发送队列** 从空变为非空可触发 **上行就绪声明**；仅有 **待上传 ready 标记** 不会直接进入 **活跃队列**
- **活跃队列** 中的客户端按 **Token Passing** 规则参与上行轮换，并可由 **接收侧上行进展判定** 在静默 grant 后移除
- 已在 **活跃队列** 中的客户端重复发出 **上行就绪声明** 时，只刷新活性，不改变既有轮换顺序
- grant 是硬截止发送机会；到期后客户端必须停止空口发送，残留数据继续保留在 **客户端上行发送队列** 中等待下一次 grant
- v6 第一切片延续固定 `duration_ms` 与固定 `guard_interval_ms` 的 **Token Passing** 调度口径，不在本阶段引入自适应扩窗
- **模型 manifest** 描述 **模型文件** 的轮次身份与完整性
- **模型文件** 与 **模型 manifest** 通过 **UFTP** 在 **下行阶段** 一发多收分发给客户端
- **下行完成屏障** 位于 **下行阶段** 与 **上行阶段** 之间
- **待上传 ready 标记** 由客户端本地生成，并与 **客户端 update 文件** 一起构成上行文件接口的输入
- **客户端 update 文件** 通过 **TCP per-client 上行** 在 **上行阶段** 提交
- **收齐结果** 描述 FL Runtime 所需的 **客户端 update 文件** 到达状态，不属于 **UFTP** 或 **Token Passing** 语义
- **接收侧上行进展判定** 只描述服务端观察到的上行数据进展，不直接表达文件是否完整完成
- **固定容量用户态队列** 与 **动态逻辑水位** 共同限定链路层缓冲行为
- **客户端上行发送队列** 和 **服务端下行发送队列** 都是 **固定容量用户态队列**，但分属不同节点角色和传输阶段
- **接收重组缓冲** 属于入站数据面路径，不承载 **Token Passing** 等待发送语义
- **链路层分流** 保证空口接收到的 **Token Passing** 授权、**上行就绪声明** 等控制语义由链路层内部处理，不写入 **TUN 设备**
- **控制面优先通路** 承接 **链路层分流** 得到的控制语义，并优先于普通数据面拥塞处理
- **链路层控制帧** 通过统一外层身份进入 **控制面优先通路**，具体控制语义不混入普通数据面流量
- **可写信用 / 反压** 把底层链路可写性传递给上层
- **可见堵塞现象** 是 **可写信用 / 反压** 受限时在上层可能观察到的结果
- 每个链路层节点（包括 server 与 client）都通过 **NODE_ID** 被唯一标识
- **链路域** 通过 WFB `channel_id` 限定空口帧所属范围，节点级目标过滤由 **NODE_ID** 与 **链路层控制帧** 完成
- **方向链路域** 将下行数据面与上行数据面分开；它避免客户端接收其他客户端上行数据面流量，但不替代 **链路层控制帧** 的节点级目标过滤
- **TCP per-client 上行** 和 **KCP per-client 上行** 都表示“每客户端独立会话”的上行语义

## Example dialogue

> **开发者:** "客户端训练完成后，是不是就直接开始上传参数？"
> 
> **领域专家:** "不对。训练完成只说明它可以发出 **上行就绪声明**；真正何时上传，要等它进入 **活跃队列** 并按照 **Token Passing** 获得发送机会。"

> **开发者:** "那 `FL Runtime / SDK` 需要理解 Token 吗？"
> 
> **领域专家:** "不需要。`FL Runtime / SDK` 只表达联邦学习文件交换语义，底层发送机会由 **Transport Backend** 之下的链路机制处理。"

## Flagged ambiguities

- "上传完成" 不能同时表示“训练完成”和“已经获得空口发送机会”；前者属于 **上行就绪声明** 前后的业务状态，后者属于 **Token Passing** 调度状态。
- "反馈" 不能混用为 **UFTP 反馈** 和通用可靠传输确认；在本项目里它们是不同语义。
- "通道" 不能混用为 **方向链路域内的链路层控制面** 和普通数据路径；前者是同一方向链路域内的控制语义处理规则，不是第三条独立 stream，后者承载文件交换语义。
- "ready" 不能同时表示 **上行就绪声明** 和 **待上传 ready 标记**；前者是控制面声明，后者是文件级标记。
- "积压" 不能混用为 **内部队列占用** 和 **可见堵塞现象**；前者描述链路层内部缓冲状态，后者描述上层能观察到的外部结果。

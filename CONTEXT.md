# 联邦学习无线空口传输上下文

本文档定义本项目的核心领域术语，用于统一讨论联邦学习场景下的无线空口传输系统。它只描述名词含义、边界和相互关系，不承载实现方案、流程规范或技术决策。

## Language

**联邦学习轮次**:
一次完整的同步联邦学习周期，包含模型发布、模型接收、本地训练、参数提交和服务端聚合。
_Avoid_: 任务批次, 会话

**轮次标识**:
用于区分不同联邦学习轮次的文件身份标识，在传输层文件契约中用于确认模型文件和客户端 update 文件属于同一轮。
_Avoid_: Token 序号, 会话 ID, 节点编号

**Token Passing**:
一种中心化发射权分配机制，用来决定哪个节点在当前时刻可以占用无线空口发送数据。
_Avoid_: 抢占发送, 自由竞争发送

**控制帧通道**:
一个独立于普通数据传输语义的控制语义通道，用于表达声明、授权和调度相关信息。
_Avoid_: 数据面, 普通上传通道

**反向控制窗口**:
主发送方向暂停后，为反方向反馈预留的短时间发送机会。
_Avoid_: 双向并发传输, 全双工反馈

**UFTP 反馈**:
客户端围绕 UFTP 下行传输回传的反馈语义，例如缺块反馈或完成状态反馈。
_Avoid_: TCP ACK, 通用确认包

**上行就绪声明**:
客户端在本地训练完成后主动声明自己已准备参与上行传输的语义。
_Avoid_: 睡眠节点自由声明, 立即上传, 自动排队成功

**活跃队列**:
当前已被纳入上行 Token 轮换、且尚未被移除的客户端集合及其服务顺序语义。客户端首次发送 **上行就绪声明** 后进入该集合；在轮换过程中即使暂时未获 Token，只要尚未被移除，仍属于该集合。
_Avoid_: 仅等待中的客户端列表, 全部客户端列表, 广播接收组

**FL Runtime / SDK**:
联邦学习程序直接调用的统一运行时接口，用于表达模型发布、模型等待、参数提交和参数收集等业务语义。
_Avoid_: 训练脚本内部传输逻辑, 直接链路层调用

**Transport Backend**:
位于 FL Runtime 之下的传输适配层，负责把联邦学习文件交换语义映射到具体传输机制。
_Avoid_: 业务层 API, 链路调度器

**应用传输层闭环**:
Transport Backend 层完成下行文件分发与上行文件提交所形成的文件交换闭环，不包含 FL Runtime / SDK 的轮次编排语义。
_Avoid_: SDK 闭环, 链路层闭环, 完整联邦学习轮次

**TUN 设备**:
位于上层 IP 流量与底层无线链路之间的虚拟网络边界。
_Avoid_: 业务协议接口, 控制帧通道

**UFTP**:
用于一发多收文件分发的双向传输协议，在本项目中主要表示服务端向多个客户端分发模型文件的下行语义，并包含客户端注册、缺块反馈和完成确认等回传语义。
_Avoid_: 点对点上传协议, 通用上行协议

**模型文件**:
服务端在下行阶段发布给客户端用于本地训练的全局模型文件。
_Avoid_: 客户端 update 文件, 控制帧, 小文件连通性探针

**模型 manifest**:
描述本轮模型文件身份与完整性的元数据文件，用于让客户端确认收到的模型文件属于正确轮次且内容完整。
_Avoid_: 模型文件本体, 运行状态机, SDK 接口

**待上传 ready 标记**:
客户端写出的文件级标记，表示本轮客户端 update 文件已经生成并可交给传输适配层提交。
_Avoid_: 上行就绪声明, 接收完成标志, 上传完成, 控制面 ready

**收齐结果**:
FL Runtime 文件接口中的状态记录，用于表达服务端是否已经获得本轮期望客户端的完整 update 文件集合。
_Avoid_: UFTP 状态, TCP 连接状态, Token 调度状态, 聚合结果

**TCP per-client 上行**:
每个客户端与服务端分别建立独立上行传输会话的上传语义。
_Avoid_: 共享上行会话, 广播上传

**客户端 update 文件**:
客户端在本地训练后提交给服务端聚合的本轮训练结果文件。
_Avoid_: 小文件连通性探针, 模型下行文件, 控制帧

**KCP per-client 上行**:
每个客户端与服务端分别建立独立 KCP 上行会话的上传语义。
_Avoid_: 共享 KCP 会话, 通用实时通道

**NODE_ID**:
用于区分不同客户端节点的逻辑标识。
_Avoid_: IP 地址, 训练轮次编号

**固定容量用户态队列**:
链路层内部用于暂存待处理数据的有限缓冲区，其核心语义是容量受限而非无限吸收上层数据。
_Avoid_: 无限缓冲, 仅内核队列

**动态逻辑水位**:
系统当前允许继续积压数据的逻辑阈值，用来表达链路层在不同时刻对缓冲占用的接受程度。
_Avoid_: 固定上限, 永久阈值

**可写信用 / 反压**:
底层链路向上层表达“当前还能不能继续写入”的能力边界。
_Avoid_: Token API, 业务显式授权

**上行完整时隙**:
单个客户端完成一次上行发送机会所对应的完整空口占用单元。
_Avoid_: 单个数据包时长, 纯发送窗口

**接收侧上行进展判定**:
服务端基于接收侧观测到的客户端上行数据变化，判断某个客户端在获得上行发送机会后是否产生传输进展的语义。
_Avoid_: 客户端显式完成消息, UFTP 反馈, 文件完成判定

**下行阶段**:
服务端主导向客户端分发模型文件的传输阶段。
_Avoid_: 上传阶段, 聚合阶段

**下行完成屏障**:
所有参与本轮同步流程的客户端都完成模型文件接收后，系统才进入后续本地训练与上行提交的同步边界。
_Avoid_: 异步先到先训, 上下行重叠, 迟到客户端策略

**上行阶段**:
客户端主导向服务端提交训练结果的传输阶段。
_Avoid_: 下载阶段, 训练阶段

## Relationships

- 一个 **联邦学习轮次** 包含一个 **下行阶段** 和一个 **上行阶段**
- 一个 **联邦学习轮次** 通过 **轮次标识** 区分其文件交付物
- **FL Runtime / SDK** 通过 **Transport Backend** 表达文件交换语义
- **应用传输层闭环** 发生在 **Transport Backend** 层，位于 **FL Runtime / SDK** 之下、链路层之上
- **Transport Backend** 通过 **TUN 设备** 与底层无线链路边界衔接
- **Token Passing** 管理 **上行阶段** 与反馈场景中的空口发送权
- **反向控制窗口** 承载与主发送方向相反的反馈语义
- **UFTP 反馈** 发生在 **下行阶段** 的反馈场景中
- **上行就绪声明** 让客户端进入 **活跃队列**，并参与后续上行发送机会分配
- **活跃队列** 中的客户端按 **Token Passing** 规则参与上行轮换与等待后续发送机会
- **模型 manifest** 描述 **模型文件** 的轮次身份与完整性
- **模型文件** 与 **模型 manifest** 通过 **UFTP** 在 **下行阶段** 一发多收分发给客户端
- **下行完成屏障** 位于 **下行阶段** 与 **上行阶段** 之间
- **待上传 ready 标记** 由客户端本地生成，并与 **客户端 update 文件** 一起构成上行文件接口的输入
- **客户端 update 文件** 通过 **TCP per-client 上行** 在 **上行阶段** 提交
- **收齐结果** 描述 FL Runtime 所需的 **客户端 update 文件** 到达状态，不属于 **UFTP** 或 **Token Passing** 语义
- **接收侧上行进展判定** 只描述服务端观察到的上行数据进展，不直接表达文件是否完整完成
- **固定容量用户态队列** 与 **动态逻辑水位** 共同限定链路层缓冲行为
- **可写信用 / 反压** 把底层链路可写性传递给上层
- 一个客户端通过 **NODE_ID** 被唯一标识
- **TCP per-client 上行** 和 **KCP per-client 上行** 都表示“每客户端独立会话”的上行语义

## Example dialogue

> **开发者:** "客户端训练完成后，是不是就直接开始上传参数？"
> 
> **领域专家:** "不对。训练完成只说明它可以发出 **上行就绪声明**；真正何时上传，要等它进入 **活跃队列** 并按照 **Token Passing** 获得发送机会。"

> **开发者:** "那 `FL Runtime / SDK` 需要理解 Token 吗？"
> 
> **领域专家:** "不需要。`FL Runtime / SDK` 只表达联邦学习文件交换语义，底层发送机会由 **Transport Backend** 之下的链路机制处理。"

## Flagged ambiguities

- "上传完成" 不能同时表示“训练完成”和“已经获得空口发送机会”；前者属于 **上行就绪声明** 前后的业务状态，后者属于 **Token Passing** 调度状态。
- "反馈" 不能混用为 **UFTP 反馈** 和通用可靠传输确认；在本项目里它们是不同语义。
- "通道" 不能混用为 **控制帧通道** 和普通数据路径；前者承载控制语义，后者承载文件交换语义。
- "ready" 不能同时表示 **上行就绪声明** 和 **待上传 ready 标记**；前者是控制面声明，后者是文件级标记。
